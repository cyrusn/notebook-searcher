[{{ range $index, $page := .Site.Pages }}
{{- if ne $page.Type "json" -}}
{{- if and $index (gt $index 0) -}},{{- end }}
{
  "index": {{ $index }},
  "kind": "{{ $page.Kind }}",
  "type": "{{ $page.Type }}",
  "uri": "{{ $page.Permalink }}",
  "title": "{{ lower (htmlEscape $page.Title)}}",
  "subtitle": "{{ lower (htmlEscape $page.Params.subtitle)}}",
  "date": "{{ dateFormat .Site.Params.dateFormat $page.Date}}",
  "tags": [{{ range $tindex, $tag := $page.Params.tags }}{{ if $tindex }}, {{ end }}"{{ lower (htmlEscape $tag) }}"{{ end }}],
  "description": "{{ lower (htmlEscape .Description)}}",
  "draft": {{$page.Draft}}
}
{{- end -}}
{{- end -}}]
